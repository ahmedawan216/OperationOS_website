import{NextResponse}from"next/server";import{requireFounderSession}from"@/lib/control-plane/auth";import{governanceActionSchema}from"@/lib/control-plane/governance";
export async function POST(request:Request){await requireFounderSession();let body:unknown;try{body=await request.json()}catch{return NextResponse.json({error:"Malformed request"},{status:400})}if(!governanceActionSchema.safeParse(body).success)return NextResponse.json({error:"Invalid governance action"},{status:400});return NextResponse.json({error:"Authoritative governance adapter is not configured"},{status:503})}
