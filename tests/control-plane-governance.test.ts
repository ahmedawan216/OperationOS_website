import assert from"node:assert/strict";import{test}from"node:test";import{executeGovernanceAction}from"../lib/control-plane/governance";import type{ApprovalLedger}from"../lib/agent-runtime/approvals";import type{ApprovalRequest}from"../lib/agent-runtime/contracts";import{fixtureSnapshot}from"../lib/control-plane/testing/fixture-provider";
const now="2026-09-22T18:00:00.000Z",pending=fixtureSnapshot.approvals[0]!,approved={...pending,status:"approved"as const};
function ledger():ApprovalLedger{let used=false;return{add(v){return v},get(){return undefined},consume(input){if(used)throw new Error("used");used=true;if(input.actionDigest!==approved.actionDigest||input.actorId!=="founder")throw new Error("mismatch");return{approvalId:approved.approvalId,candidateId:approved.subjectId,requestedBy:"runtime",actorId:"founder",actionType:"candidate.canary.start",riskLevel:"medium",approvalType:"human",actionDigest:approved.actionDigest,summary:"Exact",expiresAt:"2026-09-23T18:00:00.000Z",status:"consumed",resolvedBy:"founder",resolvedAt:now,consumedAt:now}as ApprovalRequest}}}
test("governance consumes only exact already-approved payload through Day 1 ledger",async()=>{const request={action:"consume_approved_action"as const,approvalId:approved.approvalId,subjectId:approved.subjectId,actionDigest:approved.actionDigest,actorId:"founder",consumedAt:now};const result=await executeGovernanceAction({founderId:"founder",request,approval:approved,approvals:ledger()});assert.equal("status"in result&&result.status,"consumed");await assert.rejects(()=>executeGovernanceAction({founderId:"founder",request:{...request,actionDigest:"forged"},approval:approved,approvals:ledger()}),/binding/);await assert.rejects(()=>executeGovernanceAction({founderId:"founder",request,approval:pending,approvals:ledger()}),/unavailable/)});
test("unauthorized actor fails closed",async()=>{const request={action:"consume_approved_action"as const,approvalId:approved.approvalId,subjectId:approved.subjectId,actionDigest:approved.actionDigest,actorId:"intruder",consumedAt:now};await assert.rejects(()=>executeGovernanceAction({founderId:"founder",request,approval:approved,approvals:ledger()}),/not authorized/)});
test("rollback reaches only injected authoritative controller with exact frozen target",async()=>{const canary={...fixtureSnapshot.canaries[0]!,state:"canary"as const};let called=false;const request={action:"request_canary_rollback"as const,canaryId:canary.canaryId,candidateVersionId:canary.candidateVersionId,rollbackVersionId:canary.rollbackVersionId,conditionsDigest:"sha256:"+"c".repeat(64),actorId:"founder",requestedAt:now,reason:"Operator requested bounded rollback"};const result=await executeGovernanceAction({founderId:"founder",request,canary,rollbackController:{async requestBoundRollback(){called=true;return{eventId:"rollback-event",state:"rolled_back"}}}});assert.equal(called,true);assert.equal("state"in result&&result.state,"rolled_back");await assert.rejects(()=>executeGovernanceAction({founderId:"founder",request:{...request,rollbackVersionId:"arbitrary"},canary,rollbackController:{async requestBoundRollback(){throw new Error("must not run")}}}),/binding/)});
